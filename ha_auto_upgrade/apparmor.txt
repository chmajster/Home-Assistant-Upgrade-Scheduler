#include <tunables/global>

profile ha_auto_upgrade flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>
  #include <abstractions/python>

  file,
  signal (send) set=(kill,term,int,hup,cont),
  network,
  capability net_bind_service,

  # S6 / Home Assistant base image startup
  /init ix,
  /bin/** ix,
  /usr/bin/** ix,
  /usr/local/bin/** ix,
  /run/{s6,s6-rc*,service}/** ix,
  /package/** ix,
  /command/** ix,
  /etc/services.d/** rwix,
  /etc/cont-init.d/** rwix,
  /etc/cont-finish.d/** rwix,
  /run/{,**} rwk,
  /dev/tty rw,

  # Bashio / add-on entrypoint
  /usr/lib/bashio/** ix,
  /run.sh rix,

  # Application code and Python libraries
  /data/** rwk,
  /backup/** rw,
  /app/** r,
  /usr/lib/** r,
  /usr/local/lib/** r,
  /lib/** r,
  /tmp/** rw,

  deny /proc/** wklx,
  deny /sys/** wklx,
}
